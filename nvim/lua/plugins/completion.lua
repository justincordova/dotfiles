return {
  -- Disable blink.cmp (The new default completion engine in LazyVim)
  {
    "saghen/blink.cmp",
    enabled = false,
  },

  -- Disable nvim-cmp (The old default, just in case)
  {
    "hrsh7th/nvim-cmp",
    enabled = false,
  },
}
