# My Sample Markdown file

This is an example of a markdown file with inconsistent spacing, wrong heading formatting, and unorganized lists.

## Features

- Fast formatting
- Works with multiple languages
- Supports **bold**, _italic_, and `inline code`

## Code Example

```javascript
function hello(name) {
  console.log("Hello, " + name + "!");
}
hello("World");
```
